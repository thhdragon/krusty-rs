// Tests for gcode module (moved from src/gcode/mod.rs)

#[cfg(test)]
mod tests {
    use krusty_rs::gcode::*;
    use krusty_rs::motion::MotionController;
    use krusty_rs::printer::PrinterState;
    use std::sync::Arc;
    use tokio::sync::RwLock;

    // ...existing code from gcode/mod.rs tests...
    // (Insert all #[tokio::test] functions here, as previously cataloged)
}
