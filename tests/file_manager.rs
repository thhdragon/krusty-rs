// Tests for file manager (moved from src/file_manager.rs)

#[cfg(test)]
mod tests {
    use krusty_rs::file_manager::*;
    use std::fs as stdfs;
    use std::io::Write;
    use tempfile::tempdir;

    // ...existing code from file_manager.rs tests...
    // (Insert all #[tokio::test] functions here, as previously cataloged)
}
